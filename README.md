# well-log-clustering
K-means clustering on quad combo log data

Christmas break project where I did K-means clustering on a quad combo log data. The annotations in the R Markdown file were written in Indonesian; however comments in the code were written in English.
